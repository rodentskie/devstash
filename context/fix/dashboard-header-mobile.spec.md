# Dashboard Header Spec

## Overview

Fix the mobile view of the dashboard of `apps/app`.
The header right now is too crowded, components overlapping each other.

## Requirements

- Responsive components on mobile view


## References

Check the `@context/screenshots/dashboard-header-fix.png` screenshot to see current view that needs to get fixed.
